import React from 'react';
import { View, Text, ScrollView, StyleSheet } from 'react-native';

export default function FeedScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.header}>Feed</Text>
      <ScrollView>
        {[1, 2, 3].map((post, i) => (
          <View key={i} style={styles.post}>
            <Text style={styles.postText}>Postagem #{post}</Text>
          </View>
        ))}
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
    padding: 20,
  },
  header: {
    fontSize: 28,
    color: '#fff',
    marginBottom: 20,
  },
  post: {
    backgroundColor: '#111',
    borderRadius: 10,
    padding: 15,
    marginBottom: 15,
  },
  postText: {
    color: '#fff',
  },
});
